BDD-TP2-Benjamin Van Ryseghem

1-	SELECT fnom as suppliername
	FROM Fournisseurs;

2-	SELECT *                                                             	FROM Fournisseurs                                                               	WHERE fad                                                                       	LIKE '%Paris';

3-	SELECT venom                                                             	FROM Fournisseurs                                                               	WHERE fad                                                                       	LIKE '%Paris';

4-	SELECT anom, acoul
	FROM Articles
	WHERE acoul='vert';

5-	SELECT DISTINCT aid
	FROM Catalogue
	WHERE prix < 20;

	L'ordre est changŽ, mais je ne comprends pas quel ordre est adoptŽ.

6-	SELECT DISTINCT fid
	FROM Catalogue
	WHERE prix < 20;

7- 	SELECT DISTINCT fid
	FROM Catalogue
	WHERE prix > 10
	AND prix < 20;

	SELECT DISTINCT fid
	FROM Catalogue
	WHERE prix BETWEEN 10 AND 20;

8-	SELECT DISTINCT fid
	FROM Catalogue
	WHERE prix > 10
	INTERSECT
	SELECT DISTINCT fid
	FROM Catalogue
	WHERE prix < 20;
	
9-	SELECT DISTINCT anom
	FROM Articles
	WHERE acoul='vert'
	OR acoul='rouge';

	SELECT DISTINCT anom
	FROM Articles
	WHERE acoul='vert'
	UNION
	SELECT DISTINCT anom
	FROM Articles
	WHERE acoul='rouge';

	L'ordre est diffŽrent, mais c'est tout ˆ fait normal, puisque l'union prend d'abord la premire collection puis ajoute la seconde ˆ la fin, alors que la premire solution avec une expression boolŽenne grade l'ordre puisqu'un seul passage sur la collection est effectuŽ.

10-	SELECT DISTINCT anom
	FROM Articles
	WHERE acoul='vert'
	AND acoul='rouge';

	SELECT DISTINCT anom
	FROM Articles
	WHERE acoul='vert'
	INTERSECT
	SELECT DISTINCT anom
	FROM Articles
	WHERE acoul='rouge';

	C'est deux expression peuvent sembler Žquivalente, mais sont tout ˆ fait diffŽrente. En effet, la premire requte sŽlectionne les articles a la fois rouge et vert, tandis que la deuxime requte sŽlectionne les articles propose soit en vert, soit en rouge.
	L'ŽnoncŽ de la question portant a interprŽtation, il est dur de valoriser une rŽponse plus qu'une autre.
 